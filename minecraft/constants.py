class attributes:
    load = [
        'load'
    ]  # Function will be called every time when server loads

    tick = [
        'tick'
    ]  # Function will be called every tick

    recursive = [
        'recursive'
    ]  # Optimization attribute, use it if function will be called without recursions


setupFunctionName = 'SETUP'
stackName = 'STACK'
tempPrefix = 'TMP_'
constPrefix = 'CONST_'
returnPrefix = 'RET_'
argPrefix = 'ARG_'
tArgPrefix = 'T_ARG_'
